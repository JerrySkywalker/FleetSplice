# FleetSplice

FleetSplice is an open-source control plane for coding-agent sessions across machines and inference environments.

## Status

FleetSplice Architecture 0.1 is accepted. `ARCHITECTURE_0_1_READY=true`.

Product implementation has not started. `IMPLEMENTATION_AUTHORIZED=false` and
`PRODUCT_IMPLEMENTATION_AUTHORIZED=false` remain in force until an exact-head
G04 review returns `PASS_V0_1_IMPLEMENTATION_CONTRACT`.

See [`docs/`](docs/) for the repository's architecture, research, and decision-record boundaries.

# P05 installed ZenBook14 candidate

Date: 2026-09-26. Host: ZenBook14 (`ZENBOOKS14`), non-elevated Owner account. Evidence root: `C:\Dev\artifacts\FleetSplice\FLEETSPLICE-PORTABLE-HOST-RUNTIME-TRAIN-001\P05`. Workspace: the disposable `P05\workspace` under that root. No Tencent, Casdoor admin, SKYFORGE, Windows sleep/security policy, or installed Codex mutation was involved.

The per-user NSIS candidate was built from train commit `3bb1db2ed90ab1c8924c2a4a8d6d79d44bc6676d` and installed at `%LOCALAPPDATA%\Programs\FleetSplice`. Installer SHA-256: `2b1ec97bcaa345c039c012b580623cd82a7d023841da1cdaf1173ad927774bb3`. The installed 688-file resource manifest exactly matched staging; its aggregate SHA-256 was `72d63b24722d269f7a5a209d88f8810a79da71ecab4629f2beea4d0ccf23442d`. Bundled Node was v24.20.0 / SQLite 3.53.4. The official installed Codex executable remained unchanged at SHA-256 `2d84bd03bfae0a8558719c3a043ca6f12fc438b04bc41e6259da397081223f16`.

Desktop first launch started the local Agent from bundled resources. Installed CLI reported Supervisor, Hub, Edge and admission READY. A fresh shell opened the official Codex v0.156.0 TUI through `fleetsplice codex` after exact supervised endpoint proof; the TUI was closed without an Owner prompt. Installed pause returned an unshared Codex runtime; resume returned healthy discovery. The sharing-disabled source lifecycle kept the native-server directory absent until resume, then preserved the same supervised server while sharing was paused. Startup is user controlled: Desktop uses the Tauri autostart plugin's `is_enabled` and `enable`/`disable` calls behind the Startup button; the installed HKCU Run entry was absent by default. The optional enable/disable button itself was not clicked in this unattended smoke.

For the installed fault gate, the exact app-server PID, executable and Windows process creation time were checked against the native compatibility journal before termination. Agent status changed to `RECOVERY_REQUIRED / EXITED_OR_REUSED`, and `fleetsplice codex` returned `NATIVE_SUPERVISED_NOT_READY` without opening a TUI. FleetSplice stop returned CLOSED with native exit observed. The next installed start used a new PID, endpoint identity and server incarnation, and again stopped with no owner lock. The earlier P04 real TUI/Web gate supplies same-thread command, Web reconnection and controller-fencing evidence. A separate P05 source fault retest showed the native connection-close observation change immediately after exact process termination.

The initial Desktop smoke found that Tauri's NSIS resource path was under `_up_\package-resources` and Windows PowerShell 5 left `$PSScriptRoot` empty when passed a verbatim `\\?\` path. Desktop now resolves the bundled layout and passes a normal drive path to the bundled CLI. The first stopped source run also exposed a benign `NATIVE_DISCOVERY_NOT_ATTACHABLE` record that the predecessor classifier previously rejected; the classifier now admits only explicit observation records and requires exact supervised process exit. Any FleetSplice native effect attempt remains a recovery boundary under the existing classifier and is not silently reclassified as safe.

Rollback is per user: run the installed `fleetsplice.ps1 stop`, then `%LOCALAPPDATA%\Programs\FleetSplice\uninstall.exe /S`. The observed uninstall removed the installation and preserved `%LOCALAPPDATA%\FleetSplice\G05` evidence and the installed Codex executable. Owner credentials under Codex home were not touched. The local Agent evidence should not be deleted to mask an unknown effect; a recovery-required guard remains inspectable.

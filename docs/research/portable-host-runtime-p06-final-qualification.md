# P06 portable-host final qualification candidate

Date: 2026-09-26. First live G06 Edge: ZenBook14. This record covers the local train candidate before independent exact-head review and merge into `train/pre-gate-s-development-001`. Evidence logs are under `C:\Dev\artifacts\FleetSplice\FLEETSPLICE-PORTABLE-HOST-RUNTIME-TRAIN-001\P06`.

The P00 merged base is `feac151060155df53a7661fbc15b6f7ed46d808c`. P01-P05 are serial descendants on `train/portable-host-runtime-001`; the P05 installed receipt is under the sibling `P05` artifact directory. P05's installed package was built from product commit `3bb1db2ed90ab1c8924c2a4a8d6d79d44bc6676d` and passed Desktop first launch, exact native TUI attachment, sharing pause/resume, crash fencing, fresh incarnation and proven shutdown.

The first independent read-only review of PR #14 at `7a7443ba5988d3c891ab48e23bd2a8f080a10416` found three actionable issues: managed daemon custody was incorrectly required to exit, CLI and Agent IPC timed out before cold sharing resume could complete, and HCP loss during a cold resume could leave a newly started native server. Commit `b75620933b93539f75b189f8b3787c48c172418c` corrected those paths. A new managed-daemon fixture allows its live daemon while retaining exact exit proof for Agent-supervised custody. The CLI and Agent pipe now wait beyond the Edge's bounded 120-second sharing operation. Edge checks HCP admission before startup and closes a newly started server if admission is lost before sharing commits. An isolated source CLI cold resume took 14.9 seconds and passed. The corrected installed package's cold resume took 15.2 seconds, began `NATIVE_UNSHARED`, returned healthy sharing, and stopped with exact native exit and no owner lock.

The corrected pre-merge NSIS package was built from `b75620933b93539f75b189f8b3787c48c172418c`, SHA-256 `cc707b8505b47a7c1422b7658727103d43b00d4c8fb7e0541af83eaca56608e4`. Its installed 688-file resource manifest exactly matched staging, aggregate SHA-256 `2b5bead1b8aac17f2b761f3730fa8e65adbb5a771e623d7c2649f2599d334e56`. Installed Desktop launched the Agent to READY, and sharing pause, disabled-start, cold resume, and proven stop passed. A final package from the merged head remains a separate P06 step.

`npm run check`, `npm run tokens:check` and `git diff --check` passed. The corrected focused native/portable-host suite passed: 110 tests, 104 pass, 0 fail, 6 declared skips. The corrected full serial suite passed: 301 tests, 294 pass, 0 fail, 7 declared skips. The corrected default-concurrency full suite passed with the same 301/294/0/7 counts. The first default-concurrency attempt, before the review fixes, had one failure: an isolated preflight subprocess reached its 20-second test timeout under parallel load before emitting output. The test now allows a bounded 60 seconds and explicitly checks for subprocess errors; its focused rerun and both complete full-suite reruns passed. The failed attempt is preserved as `default-suite-final.txt`; the corrected passing runs are `focused-native-reviewfix.txt`, `serial-suite-reviewfix.txt` and `default-suite-reviewfix.txt`. The first review's findings and transcript remain in the P06 artifact directory; a fresh exact-head review is still required before merge.

A scan of files changed from the P00 base found no credential-shaped `sk-proj`/GitHub tokens, literal 64-hex browser bootstrap fragments, or personal user-profile path literals. The canonical checkout remained clean. The installed Agent is stopped; no native owner lock remains. The Owner's prompt exercise, optional autostart button, and real Modern Standby sleep/wake observation have not been performed by this unattended train. Tencent remains undeployed; Gate S, live G06 acceptance, G07 and main merge remain outside this qualification.

## Second independent review and custody correction

The fresh read-only review at `652741e5392b7e078ede2080f9acd31eb72b6e11` found two merge blockers. First, the observation-only predecessor classifier admitted exactly one `NATIVE_COMPATIBILITY` row, although pause and cold resume can create several official native server incarnations in one journal. Second, a supervised foreground process lacked an OS-enforced lifetime tie to the Agent after abrupt Edge death. The second review JSON and transcript are preserved in the P06 artifact directory. PR #14 was not merged at that head.

The correction checks every recorded Agent-supervised process identity and requires exact exit before safe predecessor closure. A stale owner lock from an abruptly ended Agent can be archived only when the previous guard and journals classify safe, the lock names that exact run, its Agent PID is absent, every supervised Codex incarnation has exited, and no FleetSplice process conflicts. A mismatched lock remains blocked. The copy-before-unlink archive step is retryable after interruption.

For process lifetime, the Agent launches the official Codex foreground app-server through a FleetSplice-owned Windows Job helper. It creates Codex suspended, assigns `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, then resumes it. The helper closes the Job when the Agent's pipe closes or the native process exits. This does not modify the installed Codex binary or its managed-daemon Job guard. The helper is an inventoried resource in the per-user desktop package. [ADR 0008](../adr/0008-portable-shared-native-server-custody.md) records this custody decision.

On ZenBook14, the four real Codex supervised-server tests passed with the helper. An isolated abrupt-Edge test kept a second native client attached, killed the exact Edge, and observed exact Codex process exit; the prior guard classified `SAFE_NO_EFFECT`, its stale lock was archived and retired, and a fresh successor incarnation started and stopped with exact exit. The script and sanitized result are `abrupt-job-recovery.mjs` and `abrupt-job-recovery.txt` in P06 evidence. The focused source suite passed 145 tests (139 pass, 6 declared skips), and both serial and default-concurrency complete suites passed 302 tests (295 pass, 7 declared skips), all with zero failures. `npm run check`, `npm run tokens:check`, and `git diff --check` passed.

The corrected pre-merge NSIS package has SHA-256 `afe5430537800b46e4c5ed7c10e9b5d75e948501d1e082b960589d484ce8ac23`. Its installed 689-file resource inventory, including the Job helper, matched every staged SHA-256; aggregate `baecc51accb2aa4bb9936f224761834bcb78b1bb7884b0f0315e613045f14148`. Installed CLI start, native sharing pause/resume, exact stop, and hidden Desktop launch to READY passed. The installed Agent is stopped with a CLOSED guard and no owner lock. The official Codex SHA-256 remains `2d84bd03bfae0a8558719c3a043ca6f12fc438b04bc41e6259da397081223f16`. A fresh independent exact-head review and final merged-head package/install remain required before issuing the Owner handoff.

## Third independent review and completed-turn restart

The fresh read-only review at `1d8d0557d55fcba2b4b329bda648a5a5cf5fb23d` found one further merge blocker: a clean native Web turn still left `NATIVE_ATTACH_ATTEMPT`, `NATIVE_EFFECT_ATTEMPT`, terminal event, and receipt rows that the predecessor classifier rejected. Its exact JSON finding and transcript are preserved as `independent-review-3-final.json` and `independent-review-3-transcript.txt`. PR #14 was not merged at that head.

The correction recognizes completed native-adoption activity only when every attach and effect attempt has a unique, later successful receipt bound to the same command, thread, and qualified native artifact. Every native effect also needs a matching terminal turn event recorded after its attempt. Native command evidence must match the durable command table and every command must be terminal. An ambiguous or missing receipt, mismatched identity, nonterminal turn or command, and any live exact Agent-supervised process remain blocked. Completed effects classify `SAFE_TERMINAL`; observation-only runs retain `SAFE_NO_EFFECT`. No native command is replayed.

The real P04 ZenBook14 native journal (two server incarnations, successful same-thread attach, a completed Web submit, native events, and drained native commands) classified read-only as `SAFE_TERMINAL` with exact process exit. A copy in isolated `FS-P06-REAL-RST-1` then closed its prior guard, launched a fresh Agent/Edge to READY, and stopped cleanly. The original P04 evidence was not changed. The synthetic regression separately proves that a live exact process, ambiguous receipt, or undrained command cannot close. Evidence is `real-journal-successor.mjs` and `real-journal-successor.txt` in P06.

At this correction, the focused portable/native suite passed 146 tests (140 pass, 6 declared skips), and both complete serial and default-concurrency suites passed 303 tests (296 pass, 7 declared skips), all with zero failures. `npm run check`, `npm run tokens:check`, the source build, and `git diff --check` passed. The revised pre-merge NSIS installer SHA-256 is `3655a0d313101ed677845a6abecc2d6c800eb4a8badf8760907d662fdd14af9a`; all 689 installed resource hashes match staging, including the Job helper, with aggregate `0b9455dc580b1ead8ae28d7d2589b97e124c96f71fcb7c07e43f8acd44898784`. Installed CLI start reached `NATIVE_ADOPTED` and stopped with a CLOSED guard. A further fresh independent exact-head review and final merged-head build/install remain required.

# G05C local control parity night train

Owner authorization: `FLEETSPLICE-G05C-NIGHT-TRAIN-20260909-001`,
`G05C_LOCAL_CONTROL_PARITY_ONLY`, 2026-09-09.

The Owner's later instruction supersedes the earlier P1-only prospective
authorization, without changing historical receipts. The root Implementer is
the only product/Git writer, under an exclusive coordinator writer lease.
Independent acceptance requires a fresh separate read-only reviewer at each
exact committed and pushed head/tree; release the writer lease during review.

Serial visible stations, each requiring its predecessor's PASS:

1. P1: live model/reasoning, two real repository read-only turns, identical
   pre/post dirty-worktree fingerprints, normal start/stop and independent review.
2. P2A: explicitly registered existing Workspace roots and exact identity,
   immutable existing session targets; no Workspace lifecycle ownership.
3. P2B: exact-native Read Only, Workspace Auto and YOLO, local Host ceiling,
   requested/effective proof, disposable external fixture writes only.
4. P3: Controlled approvals (Allow Once/Deny), stale-request rejection,
   exact-turn interrupt/steer and simple left/right collapse.
5. G05C closeout: final local smoke, independent audit, accurate additive status.

Do not start G06, merge, deploy, add remote networking/mobile UI, introduce a
terminal/editor/Git IDE/worktree manager/browser, operate a second Host, alter
dotfiles/global proxy/other repositories, or discard the initial dirty P1 work.
Product write/YOLO dogfood must never target FleetSplice source. Artifacts belong
under `V:\artifacts\FleetSplice\FLEETSPLICE-G05C-NIGHT-TRAIN-20260909-001`.

Stop on architecture/Owner-decision requirements, unprovable native identity,
conflicting live runtime, journal/guard integrity failure, unknown permission
semantics, protocol-schema contradiction, generic recovery needs, secrets,
unexpected repository mutation, or unauthorized scope. At most one new protocol
repair loop per station; a second new blocking class requires Owner review.
At most two retirements across this train, solely for its own newly created
runs with EFFECT_EVIDENCE_UNBOUND, exact native exit, sound guard/admission,
intact journals and zero conflicts. Effects remain UNKNOWN; no replay or SQL
evidence edits. Other corruption is never retireable. Historical custody is
immutable. G06 planning is optional and docs-only after complete G05C PASS.

Final successful closeout must record `G05C_ACCEPTED=true`, `G06_STARTED=false`,
`IMPLEMENTATION_AUTHORIZED=NONE`, `PRODUCT_IMPLEMENTATION_AUTHORIZED=NONE`.

# FLEETSPLICE-G05C-P1-LIVE-CAPABILITY-READONLY-CODING-001

```text
OWNER_AUTHORIZATION=true
OWNER_AUTHORIZATION_SCOPE=G05C_P1_ONLY
G05C_P2_NOT_AUTHORIZED=true
G05C_P3_NOT_AUTHORIZED=true
G06_NOT_AUTHORIZED=true
```

This bounded G05C-P1 slice is limited to live native Codex capability
projection, selected-model and selected-reasoning configuration for a new
read-only native coding session, and compact native read/search/read-only
command activity projection. It does not authorize a workspace-write sandbox,
YOLO, approval UX, remote networking, Tencent, phone UI, terminal, editor,
Git UI, browser, second Host, provider migration, ACP, Admin/WSL, or generic
workstation functionality.

Acceptance requires a real normal G05B-R1 local lifecycle, live qualified
native catalog, real repository inspection on one continued native thread,
no controlled-workspace mutation, valid receipts, and `SAFE_TERMINAL` after
clean stop. The currently attempted P1 run is not an acceptance receipt:
runtime recovery is blocked and a separate Owner recovery instruction is
required before another local run may start.
